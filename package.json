{
  "name": "hotel-email-marketing",
  "version": "1.0.0",
  "description": "Hotel email marketing platform monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --recursive dev",
    "build": "pnpm --recursive build",
    "lint": "pnpm --recursive lint",
    "test": "pnpm --recursive test",
    "clean": "pnpm --recursive clean"
  },
  "keywords": ["hotel", "email", "marketing", "monorepo"],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.10.0",
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "~5.8.3"
  }
}
